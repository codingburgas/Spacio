#include "questions.h"

void questions(bool boyVoice, bool girlVoice)
{
    while (!WindowShouldClose())
    {

        BeginDrawing();

        ClearBackground(RAYWHITE);

        DrawText("Vuprosi", 190, 200, 20, LIGHTGRAY);

        EndDrawing();
    }
}
